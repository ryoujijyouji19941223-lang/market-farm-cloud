name: Market Farm

on:
  schedule:
    # GitHub cron uses UTC. These correspond to 08:07 and 20:07 JST, Mon-Fri.
    - cron: '7 23 * * 0-4'
    - cron: '7 11 * * 1-5'
  push:
    branches: [main]
    paths:
      - 'market_farm/**'
      - 'config.json'
      - 'requirements.txt'
      - 'tests/**'
      - '.github/workflows/market-farm.yml'
  workflow_dispatch:
    inputs:
      session:
        description: morning or evening
        required: true
        default: morning
        type: choice
        options:
          - morning
          - evening

permissions:
  contents: write

concurrency:
  group: market-farm-run
  cancel-in-progress: false

env:
  PYTHONPATH: ${{ github.workspace }}

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements.txt pytest
      - name: Test sensors
        run: python -m pytest -q
      - name: Decide session
        id: session
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "value=${{ inputs.session }}" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "push" ]; then
            echo "value=morning" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event.schedule }}" = "7 23 * * 0-4" ]; then
            echo "value=morning" >> "$GITHUB_OUTPUT"
          else
            echo "value=evening" >> "$GITHUB_OUTPUT"
          fi
      - name: Run market farm
        run: python -m market_farm.cli "${{ steps.session.outputs.value }}"
      - name: Save results
        shell: bash
        run: |
          git config user.name "market-farm-bot"
          git config user.email "market-farm-bot@users.noreply.github.com"
          git add data/state.json docs/index.html
          if git diff --cached --quiet; then
            echo "No changes to save"
            exit 0
          fi
          git commit -m "market farm: ${{ steps.session.outputs.value }} $(date -u +%F)"
          git pull --rebase origin "${GITHUB_REF_NAME}"
          git push origin "HEAD:${GITHUB_REF_NAME}"
